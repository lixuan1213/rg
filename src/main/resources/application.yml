server:
  port: 1985

spring:
  application:
    name: charging-station
  datasource:
    url: jdbc:mysql://localhost:3306/charging_station?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: lxLX2060492466!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

charging:
  station:
    fast-pile-count: 2
    slow-pile-count: 3
    parking-spots-per-pile: 4
  admin:
    username: admin
    password: admin123
